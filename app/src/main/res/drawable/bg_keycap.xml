<?xml version="1.0" encoding="utf-8"?>
<!-- Small keyboard-key chip used for the ↵ hint on the selected row. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/keycap" />
    <stroke android:width="1dp" android:color="@color/keycap_border" />
    <corners android:radius="6dp" />
</shape>
