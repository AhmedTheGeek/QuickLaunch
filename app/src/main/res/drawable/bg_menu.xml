<?xml version="1.0" encoding="utf-8"?>
<!-- Row context menu: the card's surface treatment at a smaller radius, so it reads as a piece of the card lifted off. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/card" />
            <corners android:radius="@dimen/menu_radius" />
        </shape>
    </item>
    <item android:top="1dp" android:left="1dp" android:right="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/card_highlight" />
            <corners android:radius="@dimen/menu_radius_inner" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/card_border" />
            <corners android:radius="@dimen/menu_radius" />
        </shape>
    </item>
</layer-list>
